DATABASE_URL="postgresql://user:password@localhost:5432/suryam_crm"

JWT_SECRET="change-this-to-a-random-secret"
JWT_ACCESS_EXPIRY="15m"
JWT_REFRESH_EXPIRY="7d"

# Generate with: node -e "const c=require('crypto'); console.log(c.randomBytes(32).toString('hex')); console.log(c.randomBytes(16).toString('hex'))"
ENCRYPTION_KEY="64-char-hex-string"
ENCRYPTION_IV="32-char-hex-string"

GMAIL_USER="your.email@gmail.com"
GMAIL_APP_PASSWORD="your-16-char-app-password"

STORAGE_DRIVER="local"
UPLOAD_PATH="./uploads"

PORT=3000
NODE_ENV="development"
RATE_LIMIT_MAX=100
